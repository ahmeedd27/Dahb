<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.image.ImageView?>

<BorderPane xmlns="http://javafx.com/javafx/23.0.1"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="org.example.store.controller.ProductViewController"
            nodeOrientation="RIGHT_TO_LEFT"
            prefHeight="700.0" prefWidth="1000.0"
            style="-fx-background-color: #f5f5f5;">

    <!-- Top: العنوان -->
    <top>
        <VBox spacing="10" style="-fx-background-color: #2196F3; -fx-padding: 20;">
            <Label text="🍔 إدارة المنتجات - المطعم"
                   style="-fx-font-size: 24px; -fx-text-fill: white; -fx-font-weight: bold;"/>
            <Label text="أضف وعدل منتجات المطعم (الساندويتشات والوجبات)"
                   style="-fx-font-size: 14px; -fx-text-fill: white;"/>
        </VBox>
    </top>

    <!-- Center: الجدول والنموذج -->
    <center>
        <VBox spacing="20" style="-fx-padding: 20;">

            <!-- جدول المنتجات -->
            <VBox spacing="10">
                <Label text="قائمة المنتجات"
                       style="-fx-font-size: 18px; -fx-font-weight: bold;"/>

                <TableView fx:id="productTable" prefHeight="250"
                           style="-fx-background-color: white; -fx-border-radius: 10;">
                    <columns>
                        <TableColumn fx:id="nameCol" text="اسم المنتج" prefWidth="250"/>
                        <TableColumn fx:id="priceCol" text="السعر (جنيه)" prefWidth="120"/>
                        <TableColumn fx:id="imageCol" text="مسار الصورة" prefWidth="300"/>
                    </columns>
                </TableView>
            </VBox>

            <!-- نموذج الإدخال -->
            <VBox spacing="15"
                  style="-fx-background-color: white; -fx-padding: 20; -fx-border-radius: 10; -fx-background-radius: 10;">
                <Label text="معلومات المنتج"
                       style="-fx-font-size: 16px; -fx-font-weight: bold;"/>

                <GridPane hgap="15" vgap="15">
                    <columnConstraints>
                        <ColumnConstraints percentWidth="20"/>
                        <ColumnConstraints percentWidth="80"/>
                    </columnConstraints>

                    <!-- اسم المنتج -->
                    <Label text="اسم المنتج:" GridPane.rowIndex="0" GridPane.columnIndex="0"
                           style="-fx-font-weight: bold;"/>
                    <TextField fx:id="nameField"
                               promptText="مثال: شاورما فراخ"
                               GridPane.rowIndex="0" GridPane.columnIndex="1"
                               style="-fx-font-size: 14px;"/>

                    <!-- السعر -->
                    <Label text="السعر:" GridPane.rowIndex="1" GridPane.columnIndex="0"
                           style="-fx-font-weight: bold;"/>
                    <TextField fx:id="priceField"
                               promptText="مثال: 35.00"
                               GridPane.rowIndex="1" GridPane.columnIndex="1"
                               style="-fx-font-size: 14px;"/>

                    <!-- اختيار الصورة -->
                    <Label text="صورة المنتج:" GridPane.rowIndex="2" GridPane.columnIndex="0"
                           style="-fx-font-weight: bold;"/>
                    <HBox spacing="10" GridPane.rowIndex="2" GridPane.columnIndex="1">
                        <Button fx:id="chooseImageButton"
                                text="اختر صورة 📷"
                                onAction="#handleChooseImage"
                                style="-fx-background-color: #FF9800; -fx-text-fill: white; -fx-font-weight: bold;"/>
                        <Label fx:id="imagePathLabel"
                               text="لم يتم اختيار صورة"
                               style="-fx-text-fill: #666; -fx-font-size: 12px;"/>
                    </HBox>

                    <!-- معاينة الصورة -->
                    <Label text="معاينة:" GridPane.rowIndex="3" GridPane.columnIndex="0"
                           style="-fx-font-weight: bold;"/>
                    <ImageView fx:id="productImageView"
                               fitWidth="150" fitHeight="150"
                               preserveRatio="true"
                               GridPane.rowIndex="3" GridPane.columnIndex="1"
                               style="-fx-border-color: #ddd; -fx-border-width: 1;"/>
                </GridPane>
            </VBox>

        </VBox>
    </center>

    <!-- Bottom: الأزرار -->
    <bottom>
        <HBox spacing="15" alignment="CENTER"
              style="-fx-background-color: white; -fx-padding: 20; -fx-border-color: #ddd; -fx-border-width: 1 0 0 0;">

            <Button fx:id="addButton"
                    text="➕ إضافة منتج"
                    onAction="#addProduct"
                    style="-fx-background-color: #4CAF50; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 30; -fx-font-weight: bold;"/>

            <Button fx:id="updateButton"
                    text="✏️ تعديل"
                    onAction="#updateProduct"
                    style="-fx-background-color: #2196F3; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 30; -fx-font-weight: bold;"/>

            <Button fx:id="deleteButton"
                    text="🗑 حذف"
                    onAction="#deleteProduct"
                    style="-fx-background-color: #f44336; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 30; -fx-font-weight: bold;"/>

            <Button text="🔄 تحديث"
                    onAction="#handleRefresh"
                    style="-fx-background-color: #9E9E9E; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 30; -fx-font-weight: bold;"/>
        </HBox>
    </bottom>

</BorderPane>